using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rayResponseComponent : MonoBehaviour
{
    public float color;
    public float type;
    public float isFlag;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
